{
  "buildCommand": "cd ../.. && npx turbo build --filter=@lombard.finance/sdk-agent && cd apps/example-agent-chat && npx vite build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/chat", "destination": "/api/chat" }
  ]
}
